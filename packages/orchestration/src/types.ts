// Runtime types are documented here for ts-node users.
// The orchestration runtime itself uses CommonJS-compatible JavaScript so it
// can also run in minimal Node environments.

module.exports = {};
