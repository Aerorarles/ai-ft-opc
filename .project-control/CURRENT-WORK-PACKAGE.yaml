work_package:
  id: M1-DB-READONLY-PREFLIGHT
  name: M1 Restricted Database Read-only Preflight
  status: BLOCKED
  started_at: "2026-07-12"
  allowed_scope:
    - read_only_database_schema_metadata
    - read_only_extension_constraint_index_permission_metadata
    - create_preflight_report_without_business_data
    - update_project_control_status_and_progress
  forbidden_scope:
    - read_config_or_env_or_credentials
    - business_data_read
    - database_write
    - migration_execution
    - production_schema_change
    - n8n_or_docker_or_server_change
    - real_outreach
    - direct_push_main
    - force_push
    - automatic_merge
  predecessor:
    id: M1-EXIT-REVIEW
    name: M1 Exit and Controlled Implementation Approval Review
    status: PASSED
    evidence: PR #10 merged to main by the user.
  blocking_condition: No local PostgreSQL read-only client or preconfigured non-secret connection entry is available. No database connection was attempted.
  next_work_package: M1-DB-READONLY-PREFLIGHT
  next_work_package_name: Resume restricted read-only database preflight
  next_work_package_requires_explicit_user_approval: false
  review_gate:
    required_action: Human provides an approved PostgreSQL client entry point with read-only access, without exposing credentials in this repository or chat.
