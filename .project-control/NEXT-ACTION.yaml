next_action:
  type: STOP
  target: M1-DB-READONLY-PREFLIGHT
  reason: User approved a restricted read-only database preflight, but no local PostgreSQL client or approved non-secret connection entry point is available. Do not guess connection parameters or read .env files.
  required_actor: user_or_environment_admin
  blocking: true
  updated_at: "2026-07-12"
  prohibited_until_resolved:
    - read_config_or_env_or_credentials
    - database_connection_without_approved_client_entry
    - business_data_read
    - migration_execution
    - production_database_write
    - production_n8n_activation
    - real_outreach
